import { StatusTarefaPipe } from './status-tarefa.pipe';

describe('StatusTarefaPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTarefaPipe();
    expect(pipe).toBeTruthy();
  });
});
